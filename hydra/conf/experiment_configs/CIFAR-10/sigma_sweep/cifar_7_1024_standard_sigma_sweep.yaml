# @package _global_

defaults:
  - /base/defaults@_global_
  - /models/cifar_7_1024@_global_
  - override /hydra/launcher: submitit_slurm_cifar10
  - override /hydra/sweeper: basic
  - _self_

random_seed: 5432
sample_size: 300
experiment_tag: "sigma_sweep"

hydra:
  sweeper:
    params:
      ++sigma: 0.25,0.5,1.0
