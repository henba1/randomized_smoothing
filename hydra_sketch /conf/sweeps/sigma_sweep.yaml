# Example sweep configuration: varying sigma values
# Usage: python certify.py --config-name certify --multirun sigma=0.25,0.5,0.75,1.0

defaults:
  - /certify@_here_

# This file extends certify.yaml and can be used for specific sweep patterns
# The actual sweep parameters are specified via command line with -m flag

