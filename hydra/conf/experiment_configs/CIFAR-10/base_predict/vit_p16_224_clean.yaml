# @package _global_
# Sets mode: base_predict to compute clean accuracy

defaults:
  - /base/defaults@_global_
  - /models/vit_p16_224@_global_
  - override /hydra/launcher: submitit_slurm_cifar10
  - _self_

mode: base_predict
random_seed: 5432
sample_size: 10000
experiment_tag: "base_predict_full_test"

