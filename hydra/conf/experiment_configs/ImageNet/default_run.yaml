# @package _global_
# Sets mode: base_predict to compute clean accuracy

defaults:
  - /base/defaults_imagenet@_global_
  - /models/beit_large_patch16_512@_global_
  - override /hydra/launcher: submitit_slurm_imagenet
  - _self_

mode: certify
random_seed: 42
sample_size: 1000
experiment_tag: "default_run"

